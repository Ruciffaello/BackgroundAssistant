# BackgroundAssistant 測試指南

這份文件列出了可以用於測試 **BackgroundAssistant** 各個階段功能的建議句子。您可以透過語音輸入這些句子，觀察系統的解析與執行結果。

## 1. 系統啟動與初始化

當程式啟動且模型載入完成後，系統會自動進行語音通知。

*   **預期行為**：在控制台顯示 `System warm-up complete` 後，語音輸出：「**暖身完畢，我已經準備好為您服務了。**」

---

## 2. 基礎功能測試 (工具執行)

這些測試旨在驗證系統是否能正確辨識意圖並執行對應的 `IMcpTool`。

| 測試目標 | 建議輸入句子 | 預期執行工具 | 預期語音回覆內容 |
| :--- | :--- | :--- | :--- |
| **本地時間** | 「現在幾點了？」 | `TimeTools` | 回報目前的本地時間。 |
| **幽默與稱讚** | 「李佩云」或「林志玲」 | `HumorTools` | 給予隨機的紳士稱讚，並詢問是否需要進一步了解。 |
| **RSS 新聞** | 「台灣總統新聞」 | `RssNewsTools` | 列出前三則新聞，並以「第X則」格式播報，標題間有明顯停頓。 |
| **卡牌搜尋** | 「幫我找噴火龍的卡牌。」 | `PtcgTools` | 回報找到關於「噴火龍」的資訊。 |
| **知識查詢** | 「幫我查詢什麼是黑洞。」 | `KnowledgeTools` | 回報這是一則知識庫查詢。 |
| **系統關閉** | 「關閉系統」或「結束程式」 | `SystemTools` | 播報「好的，助手即將關閉...」並在 2.5 秒後自動退出程式。 |

---

## 3. 多類型輸入與 CMD 測試

專案支援同時透過 **語音 (麥克風)** 與 **終端機鍵盤 (CMD)** 進行指令輸入：

*   **終端機文字輸入 (CMD Input)**：
    *   在控制台直接鍵入任何文字指令（例如 `現在幾點`、`幫我找皮卡丘`）並按 Enter。
    *   **預期行為**：手打指令直接跳過 Refiner 贅字過濾，直通 SQLite / LLM 解析，實現秒級回應。
*   **終端機快捷結束指令**：
    *   在控制台輸入 `exit`、`quit`、`q` 或 `結束`。
    *   **預期行為**：系統顯示綠色關閉提示，安全停止所有背景服務並退出。

---

## 4. 語音精煉與魯棒性測試

測試系統是否能處理口語贅字，以及對於單純姓名的識別能力。

*   **贅字移除 (Refiner)**：
    *   輸入：`「那個... 呃... 幫我查一下現在幾點。」`
    *   輸出：`「現在幾點。」` -> 觸發時間查詢。
*   **單獨姓名識別 (Name Fallback)**：
    *   輸入：`「蔡依林」` (無動詞)
    *   預期行為：`IntentParser` 會觸發長度保險機制 (2-5字)，強制進入 `HumorTools` 並提取姓名。

---

## 5. 語音播報體驗 (TTS)

測試語音合成是否自然，特別是在處理列表與數字時。

*   **新聞列表間隔**：當播報新聞時，系統應在每一則新聞標題之間停頓 **200ms**，不再連在一起讀。
*   **數字轉中文**：確保「第1則」讀作「第一則」而非「第一」。

---

## 6. 拼音熱詞校正測試 (Pinyin Correction)

測試當語音辨識出現同音異字（或發音不準）時，系統是否能自動修正為正確的專有名詞。

*   **目前支援的熱詞 (Hotwords)**：噴火龍、皮卡丘、水箭龜、妙蛙種子、超夢、快龍、關閉系統 等。

| 輸入句子 (故意發音偏差) | 預期校正結果 | 預期執行指令 (JSON) |
| :--- | :--- | :--- |
| 「幫我找 **分** 火龍」 | `噴火龍` | `{"tool": "ptcg_search", "query": "噴火龍"}` |
| 「搜尋 **比** 卡丘」 | `皮卡丘` | `{"tool": "ptcg_search", "query": "皮卡丘"}` |

---

## 7. 4GB 記憶體與推論效能檢查點

本專案已完成 4GB 記憶體極致優化：

*   **動態 Max Length**：分類器與提取器會依輸入 Token 數動態調整生成長度（上限 512），大幅縮小 KV Cache 佔用。
*   **Workstation GC**：.NET 宿主記憶體佔用極低（~50MB），總系統記憶體穩定維持在 3.2 GB 以下。
*   **輸入開關**：可在 `appsettings.json` 中設定 `"EnableSpeech": false` 進行純鍵盤極速測試。

---

## 8. 如何觀察測試結果

當您運行程式時，控制台 (Console) 會即時印出標籤：

1.  `[1. STT Result]` 或 `[1. CMD Input]`: 輸入來源與內容。
2.  `[2. Refined Text]`: 移除贅字後的文字（語音輸入時顯示）。
3.  `[3. Intent Parser Result]`: 輸出的 JSON 指令（若走快速路徑會顯示 `SQLite Fast Path`）。
4.  `[4. Execution Result]`: 工具執行產生的文字回覆。
5.  `[5. TTS Speaking]`: 最終語音合成的文字內容。
